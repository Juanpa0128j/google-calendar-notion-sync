{
  "name": "automation google calendar notion",
  "version": "1.0.0",
  "description": "Automation to send my calendar events to a notion calendar",
  "main": "index.js",
  "scripts": {
    "start": "node app.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "@notionhq/client": "^2.3.0",
    "express": "^5.1.0",
    "googleapis": "^148.0.0",
    "node-cron": "^3.0.3"
  },
  "engines": {
    "node": ">=16.x"
  }
}
